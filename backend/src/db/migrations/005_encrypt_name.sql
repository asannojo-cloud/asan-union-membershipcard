-- 개인정보 보호 3단계: 이름 암호화.
-- 로그인은 phone_hash로 조회한 단일 행에 대해서만 이름을 비교하므로 해시 컬럼은
-- 두지 않는다. 관리자 검색(부분일치)은 애플리케이션에서 복호화 후 처리한다.
-- 기존 name(평문) 컬럼은 안전 확인될 때까지 당분간 그대로 둔다.

ALTER TABLE members ADD COLUMN name_enc TEXT;
